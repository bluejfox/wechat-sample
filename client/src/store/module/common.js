// initial state
const state = {
  direction: '',
};

// getters
const getters = {
};

// actions
const actions = {
};

// mutations
const mutations = {
  direction(stateObj, val) {
    const s = stateObj;
    s.direction = val;
  },
};

export default {
  state,
  getters,
  actions,
  mutations,
};
